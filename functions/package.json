{
  "name": "pt-helper-functions",
  "private": true,
  "scripts": {
    "prebuild": "ts-node scripts/build_catalog.ts",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint src/**/*.ts",
    "setup-agent": "ts-node src/setup-managed-agent.ts",
    "setup-form-agent": "ts-node src/setup-form-agent.ts",
    "test": "jest"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "@google-cloud/billing": "^5.1.1",
    "@sendgrid/mail": "^8.1.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.9",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
